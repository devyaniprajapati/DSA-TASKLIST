#include <iostream>
using namespace std;

int main() {
    int r, c;

    // Get the number of rows and columns
    cout << "Enter the number of rows: ";
    cin >> r;
    cout << "Enter the number of columns: ";
    cin >> c;

    int arr1[r][c], arr2[r][c], sum[r][c]; // Declare 2D
